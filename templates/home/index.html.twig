{% extends 'base.html.twig' %}

{% block body %}
    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        {{ post.summary }}
        {{ post.createdAt|date('d/m/Y') }}
    {% endfor %}
{% endblock %}